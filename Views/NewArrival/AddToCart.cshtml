@model IEnumerable<OnlineJewelleryShop.Models.Cart>

@{
    ViewData["Title"] = "AddToCart";
}

<style>
 .btn-red {
  background-color: white;
  color: #ad073d;
  border: 1px solid#ad073d;
}

.btn-red:hover {
  background-color: #ad073d;
  color: white;
  border: 1px solid #ad073d;
}
    table {
        border-collapse: collapse;
        width: 100%;
    }

    th, td {
        text-align: left;
        padding: 8px;
        border: 1px solid #ddd;
    }

    th {
        background-color: #f2f2f2;
    }

    img {
        max-height: 50px;
        width: 50px;
        margin-right: 10px;
        border-radius: 50%;
    }


</style>
@*
<br />
<br />
<h2>My Shopping Cart</h2>
<hr />

<table class="table">
    <thead>
        <tr>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.Product.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <div style="display:flex; align-items:center;">
                        <img src="@item.Product.ImagePath" alt="@item.Product.ProductName" style="max-height: 50px; width: 50px; margin-right: 2px; border-radius: 50%;" />
                    </div>  
                </td>
                <td>
                    <div style="display:flex; align-items:center;">
                        @Html.DisplayFor(modelItem => item.Product.ProductName)
                     </div>
                </td>
                <td>
                    <form method="post" action="@Url.Action("UpdateQuantity", "NewArrival")">
                        @Html.Hidden("productId", item.ProductId)
                        <button type="submit" name="quantityChange" value="-" class="btn btn-outline-danger btn-red btn-sm">-</button>
                        <span>@item.Quantity</span>
                        <button type="submit" name="quantityChange" value="+" class="btn btn-outline-danger btn-red btn-sm">+</button>
                    </form>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Total)
                </td>
                <td>
                    <a href="@Url.Action("Remove", "NewArrival", new { id = item.CartId })" class="btn btn-outline-danger btn-red">Remove</a>
                </td>
            </tr>
        }

    </tbody>
</table>

<div class="form-group">
    <a asp-action="Checkout" class="btn btn-outline-danger btn-red">Checkout</a>
    <a asp-action="NewArrival" class="btn btn-outline-danger btn-red">Continue Shopping</a>
</div>
*@

<br />
<br />
<h2>My Shopping Cart</h2>
<hr />
<br />
<table>
    <thead>
        <tr>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.Product.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <div style="display:flex; align-items:center;">
                        <img src="@item.Product.ImagePath" alt="@item.Product.ProductName" />
                    </div>
                </td>
                <td>
                    <div style="display:flex; align-items:center;">
                        @Html.DisplayFor(modelItem => item.Product.ProductName)
                    </div>
                </td>
                <td>
                    <form method="post" action="@Url.Action("UpdateQuantity", "NewArrival")">
                        @Html.Hidden("productId", item.ProductId)
                        <button type="submit" name="quantityChange" value="-" class="btn btn-outline-danger btn-red btn-sm">-</button>
                        <span>@item.Quantity</span>
                        <button type="submit" name="quantityChange" value="+" class="btn btn-outline-danger btn-red btn-sm">+</button>
                    </form>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Total)
                </td>
                <td>
                    <a href="@Url.Action("Remove", "NewArrival", new { id = item.CartId })" class="btn btn-outline-danger btn-red">Remove</a>
                </td>
            </tr>
        }
    </tbody>
</table><br />
<div class="form-group">
    <a asp-action="Checkout" class="btn btn-outline-danger btn-red">Checkout</a>
    <a asp-action="NewArrival" class="btn btn-outline-danger btn-red">Continue Shopping</a>
</div>
